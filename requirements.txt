# Core application dependencies.
# Versions are pinned with a compatible-release bound so builds stay reproducible
# while still picking up patch fixes.

streamlit~=1.58
ollama~=0.6
httpx~=0.28

# Retrieval
faiss-cpu~=1.14
sentence-transformers~=5.6
langchain-text-splitters~=1.1
# numpy 2.5 requires Python 3.12, and this project supports 3.11 upward.
numpy~=2.4

# Document parsing
PyMuPDF~=1.27
python-docx~=1.2

# Configuration
python-dotenv~=1.2
